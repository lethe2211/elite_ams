<%= form_for @asset_category, :html => { :class => "form-horizontal asset_category" } do |f| %>

    <% if @asset_category.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@asset_category.errors.count, "error") %> prohibited this asset_category from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @asset_category.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@asset_category[:name]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            asset_categories_path, :class => 'btn btn-default' %>

<% end %>
